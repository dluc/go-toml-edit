# Version bump type: patch, minor, or major
bump = "patch"
# Short description of this release (required)
description = "Deterministic inline table key ordering"
# Optional context explaining why these changes were made
context = "mapToInlineTableNode now sorts keys alphabetically, fixing non-deterministic output for deeply nested maps in Marshal."
include = ["go"]
exclude = []
