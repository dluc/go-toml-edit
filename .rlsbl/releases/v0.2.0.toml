# Version bump type: patch, minor, or major
bump = "minor"
# Short description of this release (required)
description = "Add Marshal for map-to-TOML serialization"
# Optional context explaining why these changes were made
context = "strictcli needs Marshal to write TOML config files. This adds the inverse of Unmarshal for map types (struct support deferred to v2)."
include = ["go", "docs"]
exclude = []
